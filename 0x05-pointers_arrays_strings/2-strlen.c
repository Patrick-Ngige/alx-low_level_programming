#include "main.h"

/**
 * _strlen - functions to calculate the lentgh of a string
 * @n: string to be measured
 * Return: empty
 */
int _strlen(char *n)
{
int count;
for (count = 0; n[count] != '\0'; count++)
{
}
return (count);
}
